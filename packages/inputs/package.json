{
  "name": "@hexrace/inputs",
  "version": "0.0.0",
  "private": true,
  "description": "Les entrées du joueur : actions indépendantes du périphérique, sources manette / clavier / tactile, fusion en un instantané par pas.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.spec.json --noEmit"
  },
  "dependencies": {
    "@angular/core": "^22.0.0",
    "@angular/common": "^22.0.0"
  },
  "devDependencies": {
    "@angular/compiler": "^22.0.0",
    "@angular/platform-browser": "^22.0.0",
    "@types/node": "^24.0.0"
  }
}
