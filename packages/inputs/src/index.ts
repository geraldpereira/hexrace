/**
 * Les entrées du joueur. Expose les actions et l'instantané fusionné ; les sources (manette,
 * clavier, tactile) sont derrière des jetons, jamais lues directement par le jeu.
 */
export { GameInput } from '@inputs/entity/game-input';
