{
  "name": "@hexrace/camera",
  "version": "0.0.0",
  "private": true,
  "description": "La caméra qui suit le joueur : vue arrière surélevée dont la hauteur suit la vitesse, cap mêlé entre la voiture et la tuile suivante par un facteur réglable.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.spec.json --noEmit"
  },
  "dependencies": {
    "@angular/core": "^22.0.0",
    "@hexrace/commons": "*",
    "@hexrace/engine": "*",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@angular/compiler": "^22.0.0",
    "@angular/platform-browser": "^22.0.0",
    "@types/node": "^24.0.0",
    "@types/three": "^0.184.0"
  }
}
